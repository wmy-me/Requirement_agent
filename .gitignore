.venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.idea/
docs/docx/
.env
.env.*
!.env.example
*.egg-info/
.DS_Store
.coverage
htmlcov/

# 本地数据库导出（含真实数据，勿入库）
*.dump
db_data.sql

# 运行时产物：上传件 + 校准用的向量缓存（可重建，勿入库）。
# 缓存按 sha256(文本)+模型 存放，重跑校准命中即不调 API；删掉只是下次多花点调用。
storage/

# LibreOffice 打开文档时留下的锁文件
docs/.~lock.*
