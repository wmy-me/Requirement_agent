"""目标命名空间：application —— 应用服务、用例与事务编排。

当前实际代码在 src/application/（requirement_service / review_service / retrieval_service /
memory_service / decision_rules）。
子批次 4 迁移内容到本包；迁移期间保持 src.application 兼容。
"""
