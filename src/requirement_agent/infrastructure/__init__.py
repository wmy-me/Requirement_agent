"""目标命名空间：infrastructure —— 数据库、LLM、Embedding、向量、存储与渠道实现。

当前实际代码在 src/infrastructure/（db / llm / embedding / vector / parser / storage /
channels / worker）。
子批次 4 迁移内容到本包；迁移期间保持 src.infrastructure 兼容。
"""
